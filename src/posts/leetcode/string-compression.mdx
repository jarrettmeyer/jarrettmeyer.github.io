---
title: "LeetCode: String Compression"
date: 2025-11-16
description: Compress strings in place using counts of repeated characters.
tags:
  - LeetCode
  - Python
thumbnail:
  src: /images/posts/thumbnails/leetcode.svg
  alt: LeetCode Logo
---

import LeetCodeHeader from "@/components/LeetCodeHeader.astro";
import LeetCodeResult from "@/components/LeetCodeResult.astro";

<LeetCodeHeader
  difficulty="Medium"
  href="https://leetcode.com/problems/string-compression/"
/>

## Solution

I'm rather proud of this one. 😊

<LeetCodeResult runtime={0} beats={100.0} />

```python
class Solution:
    def compress(self, chars: List[str]) -> int:
        prev = None
        count = 1
        i = 0
        while i < len(chars):
            if prev == chars[i]:
                count += 1
                chars.pop(i)
            else:
                if count > 1:
                    chars[i:len(str(count))] = str(count)
                    i += len(str(count))

                prev = chars[i]
                count = 1
                i += 1

        if count > 1:
            chars[i:len(str(count))] = str(count)

        return len(chars)
```
