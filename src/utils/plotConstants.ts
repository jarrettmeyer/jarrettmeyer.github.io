/** Shared accent color for D3 distribution plots. */
export const PLOT_ACCENT_COLOR = "#5B9BD5";

/** Default aspect ratio (width / height) for D3 plot containers. */
export const PLOT_ASPECT_RATIO = 2.2;
